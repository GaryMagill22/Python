class Ninja:
# implement __init__( first_name , last_name , treats , pet_food , pet )
    def __init(self, first_name, last_name, treats, pet, pet_food) -> None:
        self.first_name = first_name
        self.last_name = last_name
        self.treats = treats
        self.pet = pet
        self.pet_food = pet_food





class Pet(Ninja): 
    def walk(Ninja):
        print(f'{self.name} {self.name} is walking his pet {self.pet}!')


    def feed(self):
        print(f'{self.first_name} {self.last_name} is is feeding {self.pet}!')

    def bathe(self):
        print(f'{self.first_name} {self.last_name} is bathing {self.pet}!')


Ninja1 = Ninja('Jackie', 'Chan', 'jelly beans', 'mini-dachshund', 'dog food' )


# implement the following methods:
# walk() - walks the ninja's pet invoking the pet play() method
# feed() - feeds the ninja's pet invoking the pet eat() method
# bathe() - cleans the ninja's pet invoking the pet noise() method
